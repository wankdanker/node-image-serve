{
  "name": "image-serve",
  "version": "2.1.1",
  "description": "Serve images from a directory, mangle them with gm and cache them",
  "main": "server.js",
  "bin": {
    "image-serve": "server.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dan VerWeire",
  "license": "MIT",
  "dependencies": {
    "debug": "^2.6.0",
    "glob": "^4.3.1",
    "optimist": "0.6.1",
    "render-sender": "^1.3.1",
    "usey-http": "^0.5.0"
  }
}
